name: ai_model_manager
description: AI 模型服务配置管理
publish_to: none

environment:
  sdk: '>=3.5.0 <4.0.0'
  flutter: '>=3.10.0'

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod:
  dio:
  flutter_secure_storage:
  shared_preferences:
  uuid:
